services:
  - type: web
    name: javafx-spacewar
    env: ubuntu
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y openjdk-17-jdk maven
    startCommand: mvn jpro:run

